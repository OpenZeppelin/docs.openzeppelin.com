[build]
  command = "yarn build"
  publish = "build/site"

# This replaces the meta refresh with an HTTP refresh that doesn't have an
# index.html suffix
[[redirects]]
  from = "/"
  to = "/openzeppelin"
  status = 302
